@page "/addpet"
@using DBL
@using Models
@using Microsoft.AspNetCore.Components.Server.ProtectedBrowserStorage
@using Microsoft.AspNetCore.Components.Forms

<h2 class="title">Add New Pet</h2>

@if (!loaded)
{
    <p>Loading...</p>
}
else if (cu == null)
{
    <p>You are not logged in.</p>
}
else
{
    <div class="form-container">

        <input class="input" placeholder="Pet Name" @bind="petName" />
        <input class="input" placeholder="Pet Address" @bind="petAddress" />

        <select class="input" @bind="selectedTypeID" @bind:after="TypeChanged">
            <option value="0">Select Type</option>
            @foreach (var t in types)
            {
                <option value="@t.TypeID">@t.Description</option>
            }
        </select>

        <select class="input" @bind="selectedRaceID">
            <option value="0">Select Race</option>
            @foreach (var r in races)
            {
                <option value="@r.PetRaceID">@r.Description</option>
            }
        </select>

        <input class="input" placeholder="Birth Year" @bind="birthYearText" />

        <InputFile class="input" OnChange="HandleFile" />

        <button class="btn" @onclick="AddPetToDB">Add Pet</button>

        <p>@message</p>
    </div>
}

@code {
    [Inject] ProtectedSessionStorage MySession { get; set; }
    [Inject] NavigationManager nav { get; set; }

    Customer cu;
    bool loaded = false;

    string petName = "";
    string petAddress = "";
    int selectedTypeID = 0;
    int selectedRaceID = 0;

    string birthYearText = "";
    string fileName = "";
    string message = "";

    List<PetType> types = new();
    List<PetRace> races = new();

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender) return;

        var res = await MySession.GetAsync<Customer>("customer");
        if (res.Success && res.Value != null)
        {
            cu = res.Value;

            PetTypeDB tdb = new PetTypeDB();
            types = await tdb.GetAllAsync();
        }

        loaded = true;
        StateHasChanged();
    }

    async Task TypeChanged()
    {
        selectedRaceID = 0;
        races = new();
        message = "";

        if (selectedTypeID == 0) return;

        PetRaceDB rdb = new PetRaceDB();
        races = await rdb.GetByTypeAsync(selectedTypeID);
    }

    async Task HandleFile(InputFileChangeEventArgs e)
    {
        try
        {
            message = "";

            var file = e.File;
            if (file == null)
            {
                fileName = "";
                message = "No file selected.";
                return;
            }

            string ext = Path.GetExtension(file.Name);
            string newName = Guid.NewGuid().ToString() + ext;

            string folder = Path.Combine(Directory.GetCurrentDirectory(), "wwwroot", "PetPics");
            Directory.CreateDirectory(folder);

            string path = Path.Combine(folder, newName);

            long maxSize = 10 * 1024 * 1024; // 10MB
            using var fs = new FileStream(path, FileMode.Create);
            await file.OpenReadStream(maxSize).CopyToAsync(fs);

            fileName = newName;
            message = "Picture selected.";
        }
        catch (Exception ex)
        {
            fileName = "";
            message = "Upload failed: " + ex.Message;
        }
    }

    async Task AddPetToDB()
    {
        if (cu == null)
        {
            nav.NavigateTo("/login", true);
            return;
        }

        if (selectedTypeID == 0 || selectedRaceID == 0)
        {
            message = "Please select type and race.";
            return;
        }

        if (string.IsNullOrWhiteSpace(fileName))
        {
            message = "Please upload a picture.";
            return;
        }

        if (!int.TryParse(birthYearText, out int birthYear))
        {
            message = "Birth Year must be a number.";
            return;
        }

        PetsDB db = new PetsDB();

        Pet p = new Pet
        {
            PetName = petName,
            PetAdress = petAddress,
            PetType = selectedTypeID,
            PetRaceID = selectedRaceID,
            PetBirthYear = birthYear,
            PetPicture = fileName,
            UpdateUserID = cu.UserID,
            IsActive = 1
        };

        var inserted = await db.InsertGetObjAsync(p);
        message = inserted == null ? "Insert failed." : "Pet added successfully!";
    }
}

<style>
    .form-container {
        width: 420px;
        margin: auto;
        padding: 30px;
        border-radius: 20px;
        background: #fff;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .title {
        text-align: center;
        margin-bottom: 20px;
        color: #2e5f3e;
    }

    .input {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        border-radius: 8px;
        border: 1px solid #ccc;
    }

    .btn {
        width: 100%;
        padding: 12px;
        background: #2e5f3e;
        color: white;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        margin-top: 10px;
        font-weight: 800;
    }
</style>
